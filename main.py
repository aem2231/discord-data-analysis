import os
from pathlib import Path
from generate_test_data import generate
from wordcloud import wordcloud
from simple_term_menu import TerminalMenu

def main():
    options = ["Yes", "No"]
    test_data_menu = TerminalMenu(options, title="Use test data?")
    #menu_entry_index is the choice made by the user. for example, if they chouse "Yes", menu entry index will equal 1
    menu_entry_index = menu.show() 
    if menu_entry_index = 1:
        new_test_data = 

main()
